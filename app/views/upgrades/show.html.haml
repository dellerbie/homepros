- content_for :title, "Successfully Upgraded - #{current_user.listing.company_name}".html_safe

.preview
  %h4
    You have successfully been upgraded to a Premium listing!
  %h4
    Here's what your Premium listing will look like in search results (click your listing to edit it):
  .listing-container.premium
    = render partial: 'listings/premium_listing', locals: { listing: current_user.listing }
